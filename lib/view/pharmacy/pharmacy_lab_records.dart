import 'package:flutter/material.dart';

class PharmacyLabRecords extends StatelessWidget {
  const PharmacyLabRecords({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("PharmacyLabRecords"),
      ),
    );
  }
}
